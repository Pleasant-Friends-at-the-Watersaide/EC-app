<!DOCTYPE html>
<html>
  <head>
    <title>ECApp</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=360,initial-scale=1">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="wrapper">
      <header>
        <nav class="navbar navbar-expand-lg navbar-dark ">
          <%= image_tag 'logogo.png', :size =>'90x70' %>
          <a class="navbar-brand" href="/">
           <%= image_tag 'ojisan.png', :size =>'70x70'%>ながのCAKE
          </a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <ul class="navbar-nav ml-auto">
              <!-- 会員側でログイン時 -->
              <% if customer_signed_in? %>
                <li class="youkoso">
                  ようこそ、<%= current_customer.last_name %>さん！
                </li>
                <div class="navbar-nav">
                  <li class="nav-item">
                     <%= link_to "マイページ", customers_my_page_path, :style =>"color:white;" %>
                  </li>
                  <li class="nav-item">
                      <%= link_to "商品一覧", items_path, :style =>"color:white;" %>
                  </li>
                  <li class="nav-item">
                      <%= link_to "カート", cart_items_path, :style =>"color:white;"%>
                  </li>
                  <li class="nav-item">
                      <%= link_to "ログアウト", destroy_customer_session_path, method: :delete, :style =>"color:white;" %>
                  </li>
              <!-- 管理者側でログイン時-->
              <% elsif admin_signed_in? %>
                  <li class="nav-item">
                     <%= link_to "商品一覧", admins_items_path, :style =>"color:white;" %>
                  </li>
                  <li class="nav-item">
                      <%= link_to "会員一覧", admins_customers_path, :style =>"color:white;" %>
                  </li>
                  <li class="nav-item">
                      <%= link_to "注文履歴一覧", admins_root_path, :style =>"color:white;"%>
                  </li>
                  <li class="nav-item">
                      <%= link_to" ジャンル一覧", admins_genres_path, :style =>"color:white;"%>
                  </li>
                  <li class="nav-item">
                      <%= link_to "ログアウト", destroy_admin_session_path, method: :delete, :style =>"color:white;"%>
                  </li>
              <!-- ログインしてない時 -->
              <% else %>
                <div class="navbar-nav">
                  <li class="nav-item">
                      <%= link_to "About", about_path, :style =>"color:white;" %>
                  </li>
                  <li class="nav-item">
                      <%= link_to "商品一覧", items_path, :style =>"color:white;" %>
                  </li>
                  <li class="nav-item">
                      <%= link_to "新規登録", new_customer_registration_path, :style =>"color:white;" %>
                  </li>
                  <li class="nav-item">
                      <%= link_to "ログイン", new_customer_session_path, :style =>"color:white;"%>
                  </li>
                </div>
              <% end %>
            </ul>
          </div>
        </nav>
      </header>
      <!--フラッシュメッセージ-->
      <% if flash[:notice] %>
        <div class="alert alert-primary" role="alert"><strong><%= notice %></strong></div>
      <% end %>

      <% if flash[:alert] %>
        <div class="alert alert-danger" role="alert"><strong><%= alert %></strong></div>
      <% end %>

      <% if flash[:error] %>
        <div class="alert alert-danger" role="alert"><strong><%= flash[:error] %></strong></div>
      <% end %>

      <main>
        <%= yield %>
      </main>
      <footer class="bg-danger text-white pt-5 pb-4">
        <div>
          <p class="text-center">ながのCAKE製作委員会</p>
        </div>
      </footer>
  </div>
  </body>
</html>